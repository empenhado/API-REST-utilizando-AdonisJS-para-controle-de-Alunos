info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3333/register
  body:
    type: json
    data: |-
      {
        "nome": "Nathan",
        "email": "nathan@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
